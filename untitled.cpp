#include "Assignheader.h"
#include <iostream>
#include "Assignheader.h"
#include <iostream>
#include <string>
#include <vector>

using namespace std;


int main()
{ 
  vector<int> carClients;

  int person```s = 4;
  string Names[] = {"Kevin", "Ronaldo", "Tina", "Beverly"};
  int Ages[] = { 22, 25, 33, 69 };

  for (int i = 0; i < person; i++)
  {
    person * a = new clients(Names[i], Ages[i]);
    carClient.push_back(a);
  }

  vector<car> cars;
  int Ages = 3;
  char * carModel[] = { "Toyota", "Subaru", "Ford" };

  for (auto V:V2)
  { cout << V << "";
  }
  cout endl
}
